# 開発方針

## PC・スマホ横画面を同時に設計する

新規・変更UIは、設計段階から以下を正式対応対象にする。PCを先に完成させて後から縮小する進め方は禁止。

- スマホ横：740×360、844×390、932×430
- PC：1366×768、1920×1080

戦闘・探索・施設・キャラクター・イベント、および将来のギルド・PvP・深淵・ガチャ等、全機能に適用する。

## 共通基盤

まず `landscape.css`、`responsive-ui.css`、`js/ui/orientationUI.js`、`js/ui/responsiveUI.js` と対象画面の既存実装を確認する。

- spacing・font scale・tap sizeは共通規則を再利用する。必要な共通値がなければ共通CSSへ段階的に集約し、画面ごとに同じ数値を散在させない。
- safe-areaは既存 `--safe-top/right/bottom/left` を再利用し、二重適用しない。
- viewportは既存の `--game-viewport-height`、モーダル用viewport変数と更新処理を使う。別のresize/orientation監視や高さ計算を各画面へ重複実装しない。
- 共通スマホ横ブレークポイントは `(orientation: landscape) and (max-height: 540px) and (max-width: 1100px)`。CSSとJSの条件を一致させる。新しい例外は必要性を確認してから追加する。
- モーダル・内部スクロール・キャラ切替等も既存コンポーネントを優先。場当たり的なmedia queryの追加、全面書き換えは禁止。

## 新規UIの設計・確認チェック

- スマホ専用配置が必要かを判断し、単なるPC全体の縮小で済ませない。
- 操作対象は44×44px以上、適切な間隔を確保する。
- 本文12〜14px以上、主要ボタン14〜16px程度を目安に実際の可読性を確認する。
- hoverなしで情報確認・操作が完結する。
- HUD・施設ラベル・コマンド・ダメージ数字・ナビが意図せず重ならない。
- 主要画面では背景／ゲーム内容を十分に残す（目標50%以上）。
- 不要な黒帯・ページ全体の縦横スクロールを出さない。長いリストは内部スクロールとする。
- ノッチ・ホームインジケータを避け、モーダルの閉じる／確認を常に操作可能にする。
- 再読み込み、縦→横、横→縦→横、ホーム往復、ブラウザバー変化後も安定する。

## 完了条件

上記5解像度すべてで対象機能を実際に表示・操作し、複数回の回転・再読み込み・画面遷移を確認する。スマホ横で操作困難なら未完成。PCだけの確認やanimation／DOMの存在だけで完了扱いにしない。

`test_responsive_ui.html` の矩形重複・画面内判定を新規UIにも必要に応じて拡張し、対象機能の既存回帰テストを実行する。スクリーンショットで可読性や背景の見え方も確認する。

実機とエミュレーションの結果を区別し、iPhone Safari／Android Chromeの実機確認、キーボード・ブラウザバー等の未確認事項は明記する。未実施テストを成功と報告しない。

## 既存仕様の保護

UI対応を理由にセーブ形式・キー・ゲームロジック・バランスを変更しない。ユーザーの未コミット変更を保護する。

ゲームコード更新時は `js/core/config.js` の `GAME_VERSION` と `UPDATE_NOTES` も更新する。開発ルール等の文書のみの変更ではゲームの更新通知を発生させない。
