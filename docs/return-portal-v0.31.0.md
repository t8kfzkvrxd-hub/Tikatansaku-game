# 帰還ポータル v0.31.0

- 画像：`assets/images/exploration/return-portal.png`。提供原本「帰還ポータル.png」を無加工コピー。SHA-256はともに `6d4ede46ff00ea2ea09ad67016030fbfa1b96dfcc3c606d39bd82505931b610e`。
- 出現：既存 `SAFE_RETURN_FLOORS` / `isSafeReturnFloor` の5F・15F…195F、計20地点。`safe_point` かつ正式階のみ専用画面を表示。緊急脱出イベントは対象外。
- 帰還：確認1回→即時UI/ロジックロック→700ms転移→既存 `returnToTown(true)`。既存の倉庫超過処理・`finalizeSafeReturn` の二重実行ガード・持ち帰り・保存を再利用。
- 続行：既存 `diveDeeperFromSafePoint()` のみ。次階・GREED+1・既存階移動効果を維持。確認キャンセルは確認を閉じ、ポータルの状態を変更しない。
- 表示：素材種類数・合計個数、G、その他アイテム数、素材詳細。通常の3択や危険イベントのUIとは分離。キャラクターのHP・気絶処理は変更なし。
- 演出：背景は固定。光の脈動4.6秒往復、魔法紋3.8秒往復、霧8秒、上昇粒子6個・4.5秒、ランタン2.7秒往復。reduced-motionでは停止/軽減。
- 確認：指定5解像度で各3回の回転、画面内・44pxボタン・スクロールなし。5秒表示の画像比較、マウス/タップによる確認表示。キャンセル不変、20連打帰還/続行が1回、素材/G持ち帰り、エルナ気絶維持、帰還後再読み込みをテスト。
- 実機Safari/Androidは未確認。Chromeエミュレーションを使用。
- `test_return_portal.html` と既存 `test_game.html` は全件成功。
- セーブキー・保存形式は無変更。現在の通常探索は途中状態全体をセーブしない既存方式のため、ポータルにいたままの再読み込み復帰は新設していない。帰還済み素材/Gは既存セーブで保持される。
