# v0.35.5 最終フル回帰

本番コード・UI・台詞・音源・テスト本体は今回変更していない。追加診断は/private/tmpの隔離Chromeスクリプトで実行。commit/pushなし。

## 既存テスト

| 保存形式 | HTML数 | PASS | FAIL |
|---|---:|---:|---:|
| 新規 | 58 | 58 | 0 |
| 旧形式 | 58 | 58 | 0 |
| v0.35完了済み | 58 | 58 | 0 |
| 合計 | 174 | 174 | 0 |

構造6項目PASS、diffチェックPASS。保存形式読込後の部品テストは共通の完了済み前提へ正規化し、新規の1F〜10F初回帰還〜施設案内は専用テストで確認。旧セーブ互換、REPLAY中立性、施設往復、画像fallbackもPASS。
出力：`/private/tmp/v0355-full-regression.json`。

## 指定5解像度の追加確認

740×360 / 844×390 / 932×430 / 1366×768 / 1920×1080。

- NPC3施設×短文/長めの既存文2ケース×5解像度＝30件PASS。本文13〜16px、最大2行、画面内、操作パネル非重複。スクリーンショットでも確認。
- BGM：各サイズでフェード停止、無音回想、終了ボタンのマウス/タッチ操作、元音量復帰、正常終了復帰PASS。既存の連続開始/中断テストもPASS。BGM二重再生は検出0、Audioインスタンスは1つ。
- SE：Web Audioの発音処理がON時1回、ミュート時0回。スピーカーによる聴感確認ではない。
- 主人公9表情×STORY/REPLAY×5サイズ＋15回の回転/再読込＝105件PASS。serious/angryの誤fallbackなし。
- ミュート＋音量を保持した画面回転は15/15 PASS。
- **再読み込み後の音設定保持は15/15 FAIL**。

## 公開条件未達：音設定の保存なし

音OFFで回想を終了し、回転後は停止状態を維持するが、ページを再読み込みすると `soundEnabled=true`、`lobbyBgm.volume=0.35` に戻る。5解像度で各3回再現。

原因は `index.html` の `let soundEnabled = true` と、`toggleSound()` に保存処理がないこと。BGMの初期音量も0.35固定。旧公開版HEADでも同じ初期化のためv0.35.5で新たに生じた回帰ではないが、今回明示された再読み込み保持要件を満たさない。音量0.23の確認は診断上の設定値で、ユーザー操作では音OFFの復元欠如が問題となる。

既存58本のPASSはこの不足を検証していなかったことによる。追加診断結果を含めて「全条件PASS」とは判定しない。
証拠：`/private/tmp/final355-audio-layout.json`（rotationMaintained=true / reload.muted=false / reload.volume=0.35）。

## 判定

**公開保留**。今回のv0.35.5の2修正・既存フル回帰はPASSだが、ミュートの再読み込み保持を別途対応してから公開条件を再確認することを推奨する。今回は修正していない。
実機Safari/Android、自動再生制限と実スピーカーの聴感は未確認。PC/スマホ結果はChromeエミュレーション。
