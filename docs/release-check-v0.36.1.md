# v0.36.1 第1章完成版・公開前フル回帰

第1章台本を正式完成版として固定。今回、本番コード・台詞・設定・画像の変更なし。commit / GitHub push なし。

## 全HTML・構造

`python3 tests/run_html.py --output /private/tmp/v0361-final-regression.json`

|保存条件|HTML本数|PASS|FAIL|
|---|---:|---:|---:|
|新規|59|59|0|
|旧セーブ|59|59|0|
|v0.35ストーリー・施設案内完了済み|59|59|0|
|合計|177|177|0|

従来58本に `test_chapter_one.html` が加わり現在59本。既存fixtureは各保存条件の読込・移行を検証した後、機能単位テスト用の共通条件を設定する。177通りの実セーブによる全編手動プレイという意味ではない。新規進行・階層別旧セーブ・完了済み状態の固有挙動は専用ストーリーテストでも確認。

`python3 test_structure.py`：6項目PASS。`git diff --check`：PASS。

## ストーリー・保存・音声

- 第1話の1F開始、4F、5F帰還門、素材、部位破壊、探索強化、9F、10F初回帰還、施設案内を確認。
- 20〜100F全9話の初回自動再生、途中再開、重複通知の無効化、再撃破時の自動再生抑止を確認。
- 100F章完了表示と101Fへの進行PASS。
- 旧セーブ到達階0 / 10 / 19 / 20 / 85 / 100 / 200の解放条件、100F以上で全話解放、既到達の会話を強制しないこと、新到達90Fの再生を確認。
- REPLAYによる所持品・HP・G・進行の変更なし、二重帰還報酬なし。
- 回想中のロビーBGM停止、終了・中断時の復帰、ミュート・音量保持、既存音声テストPASS。
- 主人公9表情、エルナ・NPC、27画像、施設背景、読込失敗時のみfallbackを既存テストで確認。
- 会話ルールは台本を再読し維持確認。エルナ単独の発言が成立し、主人公への直接回答やNPCの主人公への反応を新たに加えていない。文章の自然さは機械テストの合格とは区別する。

## 5解像度

740×360 / 844×390 / 932×430 / 1366×768 / 1920×1080。

- 第1話の全施設案内を含む147ページ×5＝735ページで、文字の縦横見切れ・左右立ち絵の重なり・会話枠外れ・横スクロール0件。
- 55代表表示で画像ロード正常、55回のマウス／エミュレートタップによる送り操作PASS。
- 会話途中の再読込・縦横回転：各サイズ3回、計15回PASS。
- 施設6表示×5、長い倉庫リスト×5、回転再読込の3回一致×5、戻る操作×5：45チェックPASS。倉庫にNPCがないのは仕様。
- スクリーンショットで可読性・人物配置を確認。
- 追加ハーネスの初回実行は第1話の回想解放不足で停止。検証データの解放フラグだけ補正して再実行し、上記結果を取得。本番不具合ではなく検証前提不足。

Chromeヘッドレス／デバイスエミュレーションでの結果。iPhone Safari・Android Chrome実機の操作、実ブラウザバー／ノッチ、端末スピーカーでの聴取は今回未確認。

## 固定・結論

台本ファイル `js/data/mainStory.js` の検証前後SHA-256は一致：

`d392856bbc2febd6711ab455569f2c95eae997d9586d8535fb652a5716188009`

検証範囲で公開阻害となる不具合なし。本番修正不要。ローカル回帰条件を満たしており公開可能と判断する。残課題は実機最終スモークテストと、既報の施設案内テンポ等の将来UX候補。台本再調整や第2章追加は行っていない。

詳細の一時出力は `/private/tmp/v0361-final-regression.json`、`/private/tmp/chapter360-visual.json`、`/private/tmp/facility352-visual.json`。これらはcommit対象外。
